#include <stdio.h> // เรียกใช้ไลบรารีสำหรับฟังก์ชัน printf

int main() { // ฟังก์ชันหลักของโปรแกรม
    printf("ชื่อ: นาย วีระชัย เฮืองฮุง\n"); // แสดงชื่อ
    printf("รหัสนักศึกษา: 016840641010-5\n"); // แสดงรหัสนักศึกษา
    printf("โรงเรียนที่จบ: วิทยาลัยเทคนิคระยอง\n"); // แสดงโรงเรียนที่จบ
    return 0; //จบการทำงาน
}

